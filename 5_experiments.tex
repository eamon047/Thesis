\section{Experiments}
\label{chap:experiments}

This chapter will present the main empirical results.

\subsection{Experimental Setup}

The experiments use FB15k-237, which contains 14,541 entities and 237
relations. Its training, validation, and test sets contain 272,115, 17,535,
and 20,466 triples, respectively. We consider two KGE model families, RotatE
and TransE. They provide two different model settings for checking whether the
observed relation-level patterns are specific to one embedding model.

For each model family, one trained model is first used as the reference model.
Additional models are then trained with the same model configuration and
different random seeds. Let \(H_0\) be the filtered Hits@10 of the reference
model and \(H_i\) the filtered Hits@10 of a candidate model. A candidate is
included in the comparable model set when
\begin{align}
    |H_i-H_0| < \epsilon,
    \qquad \epsilon=0.01.
\end{align}
This process is repeated until the model outputs required for the multiplicity
analysis have been collected. Figure~\ref{fig:experimental-workflow} summarizes
this procedure. The performance condition keeps the comparison focused on
models with similar aggregate link-prediction performance, while the different
random seeds allow their query-level decisions to vary.

\begin{figure}[t]
    \centering
    \setlength{\fboxsep}{7pt}
    \begin{tabular}{c}
        \fbox{\parbox{0.72\textwidth}{\centering
        Train a reference model and record its filtered Hits@10, \(H_0\)}}\\[4pt]
        \(\Downarrow\)\\[-1pt]
        \fbox{\parbox{0.72\textwidth}{\centering
        Train a candidate from the same model family and configuration\\
        using a different random seed}}\\[4pt]
        \(\Downarrow\)\\[-1pt]
        \fbox{\parbox{0.72\textwidth}{\centering
        Compare aggregate performance:
        \(\lvert H_i-H_0\rvert < \epsilon\), where \(\epsilon=0.01\)\\
        Retain the candidate if the condition is satisfied; otherwise train
        another candidate}}\\[4pt]
        \(\Downarrow\)\\[-1pt]
        \fbox{\parbox{0.72\textwidth}{\centering
        Repeat until the comparable model set is formed}}\\[4pt]
        \(\Downarrow\)\\[-1pt]
        \fbox{\parbox{0.72\textwidth}{\centering
        Compute filtered top-10 decisions and relation-level
        Hits@10, ambiguity, and discrepancy}}\\[4pt]
        \(\Downarrow\)\\[-1pt]
        \fbox{\parbox{0.72\textwidth}{\centering
        Analyze multiplicity by relation structure and prediction side}}
    \end{tabular}
    \caption{Experimental workflow for constructing a comparable model set and
    carrying out the relation-level multiplicity analysis.}
    \label{fig:experimental-workflow}
\end{figure}

All link-prediction outputs are evaluated under the filtered setting with
\(k=10\). The analysis uses the raw outputs before voting aggregation, denoted
by the \texttt{without} condition in the experiment files. For each relation,
we report the relation-level Hits@10, ambiguity, and discrepancy defined in
Chapter~\ref{chap:framework}. The main figures and grouped comparisons retain
relations with at least ten test triples
(\(\texttt{test\_support}\geq 10\)); relations below this threshold are not
used in the displayed comparisons.

\subsection{Mapping Type Analysis}
\label{sec:mapping_type_analysis}

TODO: Write the main positive result.
This section should emphasize that the strongest pattern appears when mapping type is analyzed by prediction side.

\subsubsection{Combined Relation-Level View}

TODO: Briefly explain why the combined view is less informative.

\subsubsection{Side-Aware Results}

TODO: Present the head-side and tail-side results for RotatE and TransE.

\subsubsection{Cross-Model Comparison}

TODO: Explain that RotatE and TransE show the same directional pattern.

\subsection{Inverse-Like Support Analysis}
\label{sec:inverse_analysis}

TODO: Write this as an exploratory secondary result.
The intended conclusion is that inverse-like support is not a global monotonic explanation, but stricter high-confidence inverse-like metrics identify a small stable subgroup.

\subsection{Symmetry Analysis}
\label{sec:symmetry_analysis}

TODO: Write this as a weak or negative result.
The intended conclusion is that symmetry is plausible but does not provide a clean standalone explanation after accounting for self-loop sensitivity.

\subsection{Relation Frequency Control}
\label{sec:relation_frequency_control}

TODO: Pending.
Only write this section if the result and thesis role are confirmed.
